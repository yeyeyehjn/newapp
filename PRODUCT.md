# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **仲裁员**（当前唯一已实现角色）：如画像「张明 · 第六届仲裁员」，以首席 / 独任 / 边裁身份办案；在手机上处理开庭、审阅、签署、审批等日常事务。
- **多角色规划中**（已确认）：后续将增加秘书端 / 管理端等角色，信息架构不得锁死单角色假设；各角色边界尚未定义（开放决策）。

## Product Purpose

「穗仲云」广州仲裁委员会仲裁员端智能管理平台：让仲裁员在移动端独立完成办案全流程——工作台、案件管理与详情、待办任务、庭审笔录 / 文书 / 声明承诺书签署、延期开庭审批、草拟裁决、酬金查询、聘书管理、案件合议讨论、消息通知与数据统计。成功 = 仲裁员不依赖 PC 或秘书线下流转即可完成日常办案操作。

## Positioning

仲裁机构的官方移动办案工具：仲裁行业流程与术语（合议、笔录签署、回避声明、聘书、裁决草拟、延期审批）为内置能力而非外挂配置；案件保密与个人信息敏感性要求机构级安全能力（双因子登录、脱敏展示）。

## Operating Context

- **交付形态**（已确认）：最终以**微信小程序**交付；本仓库为 Web 原型 / 预览，以 400×840 手机壳（`MiniProgramContainer`）模拟小程序环境，首要目标平台为微信内置浏览器。
- **使用场景**：仲裁员在开庭间隙、审阅文书、处理待办时使用；涉及保密案件材料，手机号 / 证件号 / 银行卡号须脱敏展示。
- **登录**：双因子两步流程——① 人脸识别登录（手机验证码 → 人脸识别）；② 账号密码登录（账号密码 → 绑定手机号或邮箱验证码校验）。
- **数据阶段**（已确认）：Mock 演示阶段，全部数据来自 `src/data/mockData.ts` 及组件内置 mock；对接真实后端时须保持相同数据结构，不得虚构真实接口已存在。
- **导航模型**：状态驱动单页（`activeTab` + `activeSubPage`），不引入路由库；二级子页面须有明确返回链。

## Capabilities and Constraints

- 已实现模块：工作台（v1/v2 双版本）、案件列表与详情、待办中心、统计中心、合议讨论、聘书、酬金单、消息通知、个人信息 / 工作 / 银行信息编辑、四类签署流程（笔录 / 文书 / 声明 / 草拟裁决）、延期审批、待开庭列表、登录页（已挂载为应用入口，深色「一键核签」占位屏已删除）。
- 约束：中文单语言（无 i18n 计划）；无暗色模式；样式使用 Tailwind 4 语义 Token（见 DESIGN.md），禁止硬编码颜色；质量门槛为 `npm run lint`（tsc 零错误）+ `npm run build` 双通过。
- 未挂载组件：`ArbitrationKnowledge.tsx`、`Workbench copy.tsx`（历史副本，去留为开放决策）。

## Brand Commitments

- 产品名：**穗仲云**（`index.html` 标题）；机构署名：**广州仲裁委员会**。
- 顶部徽标字样：「仲裁员端」；品牌资产位于 `public/tu/`（`logo1.png`、`new-banner.png` 等 14 张）。
- 机构级正式语态：文案克制、专业，安全相关话术（CA 盾、双因子、保密提示）为既有品牌事实。

## Evidence on Hand

- Mock 数据域（`src/data/mockData.ts` + `src/types.ts`）：案件 5 条、任务 3 条、笔录签署 5 条、延期审批 4 条、仲裁员画像 1 份，及酬金 / 声明 / 文书 / 草拟裁决 / 待开庭等组件内置数组。
- 文档：`穗仲云仲裁员_PRD_V1.0.md/.html`（根目录）、`DESIGN.md`（完整设计规范）、`CRITIQUE.md`。
- 缺失且不得虚构：真实后端接口、真实用户证言、真实办案统计、真实客户案例。

## Product Principles

1. **移动办案优先**——一切设计以仲裁员在手机上独立完成操作为准绳。
2. **机构级安全可信**——双因子登录、敏感信息脱敏、保密话术是产品能力，不是装饰。
3. **小程序原生习惯**——微信交互范式（返回、Tab、弹层、热区）优先于自创模式。
4. **Mock 即契约**——演示阶段的数据结构就是未来接口契约，结构变更须视为接口变更。
5. **单角色起步、多角色预留**——仲裁员端先立住，命名与信息架构为秘书端 / 管理端留出空间。

## Accessibility & Inclusion

- 对比度遵循 WCAG AA（正文 ≥ 4.5:1，见 DESIGN.md §6.6）；动画全局尊重 `prefers-reduced-motion`（`src/index.css`）；可交互元素提供 `aria-label` / `aria-current`，触摸热区 ≥ 44×44px。
