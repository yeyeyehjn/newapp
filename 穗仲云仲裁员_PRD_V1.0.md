# 广州仲裁委员会 · 穗仲云仲裁员功能 PRD

## 开发人员

| 产品  | 前端     | 后端     | 测试     |
| --- | ------ | ------ | ------ |
| 黄佳楠 | <br /> | <br /> | <br /> |

## 一、基础信息

| 项目   | 说明                                                        |
| ---- | --------------------------------------------------------- |
| 对应原型 | 微信小程序：首页工作台、案件列表、案件详情、任务中心、声明承诺书、笔录签名、延期审批、文书签名等 17 个功能模块 |
| 接入方式 | 微信小程序，底部 4 Tab 导航（首页 / 案件 / 待办 / 我的）+ 多级子页面               |
| 操作角色 | 广仲在聘仲裁员（首席 / 独任 / 边裁）                                     |
| 所属模块 | 穗仲云 · 仲裁员移动端                                              |
| 页面入口 | 微信小程序首页（工作台），底部 Tab 切换至案件、待办、我的                           |
| 核心目标 | 仲裁员在移动端完成案件办理、待办处理、文书签章、庭审排期、延期审批、数据统计、个人档案管理等全流程仲裁业务     |

## 二、角色权限与统一规则

### 2.1 角色权限

- 查询该仲裁员负责的案件、待办事项。

### 2.2 统一规则

**签名规则：**

- 手写签名采用 HTML5 Canvas 画板，支持触摸与鼠标手写。

**列表页通用规则：**

- 顶部搜索框 + 状态筛选 Tab（每个 Tab 带计数角标）。
- 卡片式列表，状态徽章配色统一：待处理-琥珀色、已完成-绿色、已驳回-红色。
- 空状态占位提示 + `清空搜索与过滤` 操作按钮。

**导航规则：**

- 所有子页面顶部采用微信小程序风格返回栏（左侧 `返回` + 居中标题）。
- 底部 4 Tab 常驻（首页 / 案件 / 待办 / 我的），待办 Tab 显示未处理数量红色徽章。

**数据展示规则：**

- 金额智能格式化（超千万显示 `千万元`，超万显示 `万元`）。
- 时间格式化与时间轴展示。
- 统计图表采用 SVG 自绘（环形图、柱状图、进度条）。

**本期暂不做：**

- 智云数字法庭的实时开庭功能为概念展示，正式开庭能力待后续版本对接。
- 裁决书全文在线编辑（当前仅预览 + PDF 附件上传）。

**其他规则：**

- 登录采用账号密码+手机/邮箱验证码或手机号码+人脸双因素校验，登录成功后自动跳转到首页工作台。
- 退出登录需二次确认。

## 三、登录

### 3.1 页面结构

登录页采用「方式选择 → 多步表单」两级结构，先选择登录方式，再进入对应的双因素验证流程。

| 区块    | 规则                                                        |
| ----- | --------------------------------------------------------- |
| 品牌区   | 顶部展示「穗」Logo 图标 +「广州仲裁委」品牌标识 + 标题「仲裁员端 · 安全登录」+ 引导文案       |
| 方式选择区 | 两张可选卡片：① 人脸识别登录（手机验证码 + 人脸活体识别）；② 账号密码登录（账号密码 + 绑定手机短信验证） |
| 步骤指示器 | 进入表单后顶部展示 Stepper 进度条，标注当前步骤（共 2 步）                       |
| 表单区   | 根据所选方式与步骤，动态展示对应字段（详见 3.2）                                |
| 操作区   | `下一步` / `安全登录` 主按钮 + 协议勾选 + 忘记密码入口                        |
| 安全提示  | 底部展示「CA 盾加密通道 · 数据全程脱敏」                                   |

### 3.2 字段规则

#### 3.2.1 人脸识别登录

| 步骤    | 字段     | 组件        |  必填 | 规则                               |
| ----- | ------ | --------- | :-: | -------------------------------- |
| 第 1 步 | 手机号码   | 输入框（tel）  |  是  | 11 位手机号，校验正则 `^1[3-9]\d{9}$`     |
| 第 1 步 | 短信验证码  | 输入框（tel）  |  是  | 6 位纯数字，右侧附「获取验证码」按钮（60s 倒计时）     |
| 第 2 步 | 已验证手机号 | 只读展示      |  —  | 回显第 1 步手机号，带「已验证」标识              |
| 第 2 步 | 人脸识别   | 采集区（点击触发） |  是  | 点击开始活体采集，采集成功后显示「已采集」标识，支持「重新采集」 |

#### 3.2.2 账号密码登录

| 步骤    | 字段      | 组件            |  必填 | 规则                                    |
| ----- | ------- | ------------- | :-: | ------------------------------------- |
| 第 1 步 | 仲裁员账号   | 输入框           |  是  | 去空格后长度 ≥ 2                            |
| 第 1 步 | 登录密码    | 输入框（password） |  是  | 长度 ≥ 6，右侧附「显示/隐藏」切换按钮                 |
| 第 1 步 | 忘记密码    | 文本按钮          |  否  | 右下角入口，跳转找回流程                          |
| 第 2 步 | 账号绑定手机号 | 只读展示          |  —  | 系统根据账号自动回填绑定手机号（脱敏显示，如 `139****1234`） |
| 第 2 步 | 短信验证码   | 输入框（tel）      |  是  | 6 位纯数字，右侧附「获取验证码」按钮（60s 倒计时）          |

### 3.3 交互规则

**方式选择：**

- 默认展示登录方式选择页，点击任一卡片进入对应登录流程。
- 表单页左上角「返回」按钮：第 2 步返回第 1 步并清空验证码/人脸状态；第 1 步返回方式选择页并重置全部表单。

**人脸识别登录：**

- 第 1 步：校验手机号格式 + 验证码为 6 位 → 通过后进入第 2 步；失败则「下一步」按钮置灰。
- 第 2 步：点击采集区开始人脸活体识别（约 2.2s），采集完成后显示「人脸采集成功」。
- 点击「安全登录」：校验手机号、验证码、人脸均已采集 → 调用 `onLogin` 写入登录态。

**账号密码登录：**

- 第 1 步：校验账号（≥ 2 字符）+ 密码（≥ 6 位）→ 通过后系统根据账号查询绑定手机号（脱敏展示），进入第 2 步；失败则「下一步」按钮置灰。
- 第 2 步：向绑定手机发送验证码（60s 倒计时），校验验证码为 6 位。
- 点击「安全登录」：校验验证码通过 → 调用 `onLogin` 写入登录态。

**验证码：**

- 「获取验证码」按钮仅在手机号合法且倒计时为 0 时可点击；点击后进入 60s 倒计时，倒计时期间按钮文案变为「Xs 后重发」。

**登录成功：**

- 登录成功后自动跳转至首页工作台。

## 四、工作台首页

### 4.1 页面结构

| 区块        | 规则                                                                           |
| --------- | ---------------------------------------------------------------------------- |
| 顶部品牌区     | 广仲品牌 Banner 图；`消息` 快捷入口（红色未读圆点，跳转消息通知页）；小程序控制组件（可切换 v1/v2）                   |
| 主题 Banner | `立信铸就广仲 · 创新赢得未来` 主题宣传卡片，含 3D 建筑示意图                                          |
| 核心数据卡片    | Bento 网格布局：我的待办（左大卡，红色徽章，跳转待办 Tab）、在办案件（右上，跳转案件列表筛选审理中）、累计结案（右下，跳转案件列表筛选已结案） |
| 最新动态      | 垂直滚屏跑马灯，每 4 秒自动滚动一条新闻/通知标题，点击查看动态详情弹窗；右侧分页指示点可手动切换                           |
| 近 3 天待开庭  | 时间轴卡片，展示今天/明天/后天开庭状态徽章、案号、当事人、开庭时间地点、办案秘书；默认 3 场，可展开更多/收起；点击进入案件详情           |
| 近期待办      | 9 宫格图标入口，带未处理数量红色徽章：声明承诺书、笔录签名、延期审批、文书签名、草拟裁决书                               |
| 常用功能      | 统计中心、我的聘书                                                                    |
| 为你精选      | 推荐文章卡片（仲裁员须知/审理指引标签），`换一换` 轮换，`更多内容` 打开仲裁指引弹窗                                |
| 弹窗区       | 动态详情弹窗、裁决书模版与示范案例库弹窗、高效办案指引弹窗、智慧广仲云平台入口弹窗、仲裁员须知弹窗、智云数字法庭（概念展示）               |

### 4.2 展示规则

| 区块     | 字段/元素  | 规则                        |
| ------ | ------ | ------------------------- |
| 核心数据卡片 | 我的待办数量 | 红色徽章显示数字，点击跳转待办 Tab       |
| 核心数据卡片 | 在办案件数  | 实时取值，点击跳转案件列表（预设筛选 `审理中`） |
| 核心数据卡片 | 累计结案数  | 实时取值，点击跳转案件列表（预设筛选 `已结案`） |
| 最新动态   | 滚动间隔   | 4 秒/条                     |
| 最新动态   | 动态详情弹窗 | 展示标题、发布日期、信源、正文           |
| 待开庭日程  | 展示数量   | 默认 3 场，`展开更多` 查看全部        |
| 近期待办   | 角标数量   | 对应各任务模块的待处理数，实时更新         |

### 4.3 交互规则

- 点击核心数据卡片 → 跳转对应列表页（带预设筛选条件）。
- 点击最新动态条目 → 打开动态详情弹窗。
- 点击待开庭时间轴卡片 → 进入对应案件详情。
- 点击近期待办 9 宫格图标 → 跳转对应任务列表。
- 点击 `换一换` → 轮换推荐文章；点击 `更多内容` → 打开仲裁指引弹窗。

## 五、案件列表

### 5.1 页面结构

| 区块     | 规则                                                                        |
| ------ | ------------------------------------------------------------------------- |
| 搜索区    | 按案号、标题、申请人、被申请人关键词模糊匹配                                                    |
| 状态 Tab | 全部 / 在办案件（审理中）/ 已结案件，每个 Tab 带计数角标                                         |
| 筛选抽屉   | 仲裁员类型（独任、首席、边裁）、经办秘书、标的、开庭日期、结案方式（裁决、调解、撤案）、快捷筛选（重大案件、即将延期、已延期）；支持 `重置筛选` |
| 列表区    | 卡片式展示，按组庭时间倒序                                                             |
| 空状态区   | 空状态提示 + `清空搜索与过滤` 按钮                                                      |

### 5.2 卡片展示规则

| 字段    | 规则                 |
| ----- | ------------------ |
| 案号    | 完整展示               |
| 状态徽章  | 审理中  / 已结案，配色见统一规则 |
| 申请人   | 名称展示               |
| 被申请人  | 名称展示               |
| 争议金额  | 智能格式化（超千万显示 `千万元`） |
| 办案秘书  | 姓名                 |
| 立案时间  | YYYY-MM-DD         |
| 组庭时间  | YYYY-MM-DD         |
| 仲裁员角色 | 标签：首席 / 独任 / 边裁    |
| 纠纷类别  | 文字标签               |

### 5.3 交互规则

- 输入关键词 → 实时模糊匹配刷新列表。
- 切换状态 Tab → 按对应状态过滤。
- 打开筛选抽屉 → 选择纠纷类别，点击 `重置筛选` 清空类别条件。
- 点击案件卡片 → 进入案件详情。

## 六、案件详情

### 6.1 页面结构

| 区块        | 规则                                                     |
| --------- | ------------------------------------------------------ |
| 返回栏       | 左侧 `返回` + 居中案号标题                                       |
| 渐变 Banner | 展示案号、案由、案件状态，右侧显示该仲裁员在该案件的待办数，点击待办数显示待办清单，点击进入对应的待办详情页 |
| Tab 导航    | 可横向滑动，6 个 Tab：基本信息 / 当事人 / 仲裁请求 / 主要材料 / 文书签名 / 裁决书核阅  |

### 6.2 各 Tab 展示规则

| Tab   | 展示内容                                                                                                   |
| ----- | ------------------------------------------------------------------------------------------------------ |
| 基本信息  | 关键信息网格：立案日期、组庭日期、争议金额、仲裁费、办案秘书；秘书联系方式（电话/邮箱，点击一键复制，反馈 `已复制`）；案情摘要（AI 生成，带 `AI生成，仅供参考` 水印）              |
| 当事人   | 申请人 / 被申请人两区块，显示人数；每位当事人：属性（企业/自然人）、名称、证件类型及号码、手机、邮箱、法定地址                                              |
| 仲裁请求  | 事实和理由完整陈述；请求项编号列表（违约金、返还投资款、赔偿损失、仲裁费承担等）                                                               |
| 主要材料  | 材料搜索（名称/类别/提交方）；按类别分组：申请书、申请人证据、被申请人证据、申请人答辩状、被申请人答辩状、其他材料；点击弹出预览弹窗，提供下载                               |
| 文书签名  | 庭审笔录 PDF（从网上签名处获取）；仲裁裁决书 PDF（待签名/已签名）；签名流程：确认签名 → 成功反馈（提示语“签署成功”）                                      |
| 裁决书核阅 | 案件头部信息；文书概览（仲裁请求、答辩意见、举证质证）；核阅流转记录时间轴（角色头像/时间/意见/附件）；裁决书草稿版本管理（v1/v2 切换）；上传文书表单（提醒对象/备注/附件）；PDF 全屏预览弹窗 |

### 6.3 交互规则

- 点击 Tab 标题 → 切换展示内容，支持横向滑动。
- 点击秘书电话/邮箱 → 复制到剪贴板，Toast `已复制`。
- 点击材料条目 → 弹出预览弹窗。
- 文书签名 Tab → 走签名流程（见 6.4）。
- 裁决书核阅 Tab → 走核阅/上传流程（见 6.5）。

### 6.4 文书签名流程

- 确认签名：点击后 loading 动画，调用 GDCA 签章接口
- 签名成功：提示“签署成功”，并刷新签署状态

### 6.5 裁决书核阅流程

| 步骤 | 操作       | 规则                                 |
| -- | -------- | ---------------------------------- |
| 1  | 查看核阅流转记录 | 时间轴展示：秘书发起 → 一级核阅（退回/通过）→ 秘书修改中    |
| 2  | 查看裁决书草稿  | 支持 v1/v2 版本切换，查看裁决书全文              |
| 3  | 上传文书     | 选择提醒对象、填写备注、添加裁决书附件及其他附件，点击 `提交上传` |
| 4  | PDF 预览   | 全屏深色背景，模拟裁决书内容，带水印与页码，提供下载         |

## 七、待办中心

### 7.1 页面结构

| 区块      | 规则                                   |
| ------- | ------------------------------------ |
| 统计概览卡片  | 6 张卡片纵向排列，每张可点击进入对应列表页面（独立子页面）       |
| 详情子页面   | 5 个子页面，分别对应 5 类待办的详情列表               |
| 任务处理流程页 | 3 种任务类型（裁决书签署 / 开庭排期 / 材料审阅）的处理弹窗/页面 |

### 7.2 统计概览模块

待办中心顶部为统计概览区，共 6 张卡片纵向排列。每张卡片统一结构：左侧图标+标题、右侧待办总数（大字数字）+ 箭头、底部统计指标网格及进度条。点击整张卡片跳转至对应独立列表页。

#### 7.2.1 待开庭提醒（红色）

| 元素           | 规则                                                                                                  |
| ------------ | --------------------------------------------------------------------------------------------------- |
| 图标           | 红色背景，法槌图标                                                                                           |
| 待办总数（右上角大字）  | 待开庭总数 = 今日开庭数 + 临近 + 已排期数                                                                              |
| 统计网格（3 列）    | **今日**（红色）：开庭时间是今日的开庭数据<br />**临近**（琥珀色）：开庭时间是明天的开庭数据<br />**已排期**（绿色）：后天及之后的开庭数据 |
| 本周开庭完成率      | 底部进度条 + 文案；规则：`本周已开庭数 ÷ 本周总开庭数 × 100%`；当临近待排期数为 0 时显示 100%                                           |
| 点击跳转         | → 待开庭列表（`pendingHearingList`）                                                                       |

#### 7.2.2 声明承诺书（青色）

| 元素           | 规则                                                       |
| ------------ | -------------------------------------------------------- |
| 图标           | 青色（teal）背景，文件盾牌图标                                        |
| 待办总数（右上角大字）  | 未签署声明承诺书数量                                               |
| 统计网格（2 列）    | **待签署**（青色）：状态为未签署的承诺书数量<br />**已签署**（绿色）：状态为已签署的承诺书数量 |
| 点击跳转         | → 声明承诺书列表（`declarationList`）                             |

#### 7.2.3 笔录签名（橙色）

| 元素           | 规则                                                       |
| ------------ | -------------------------------------------------------- |
| 图标           | 橙色背景，钢笔尖图标                                               |
| 待办总数（右上角大字）  | 未签名笔录数量                                                  |
| 统计网格（2 列）    | **待签署**（橙色）：状态为未签名的笔录数量<br />**已签署**（灰色）：状态为已签名的笔录数量  |
| 点击跳转         | → 笔录签名页（`transcriptSignature`）                           |

#### 7.2.4 延期审批（琥珀色）

| 元素           | 规则                                                                                     |
| ------------ | -------------------------------------------------------------------------------------- |
| 图标           | 琥珀色背景，时钟图标                                                                             |
| 待办总数（右上角大字）  | 待审批（状态为 pending）的延期申请数量                                                                |
| 统计网格（2 列）    | **待审批**（琥珀色）：状态为 pending 的延期申请数<br />**已审批**（绿色）：状态为 approved 或 rejected 的延期申请数 |
| 点击跳转         | → 延期审批页（`postponementApproval`）                                                        |

#### 7.2.5 文书签名（靛蓝色）

| 元素           | 规则                                                                                                                       |
| ------------ | ------------------------------------------------------------------------------------------------------------------------ |
| 图标           | 靛蓝色背景，文件签名图标                                                                                                             |
| 待办总数（右上角大字）  | 待签文书数 + 固定偏移量                                                                                                            |
| 统计网格（2 列）    | **待签**（靛蓝色）：当前待签署的裁决书等文书数<br />**本月已签**（天蓝色）：本月已完成 GDCA 签章的文书数                                            |
| 年度完成率进度条     | 底部进度条 + 文案；规则：`本年已签数 ÷ 本年度应签数 × 100%`；当待签数为 0 时进度条满格显示 100%<br />本年度应签数 = 该仲裁员名下点击结案时间为本年且需其签名的文书总数 |
| 点击跳转         | → 文书签名列表（`docSignatureList`）                                                                                            |

#### 7.2.6 草拟裁决书（玫红色）

- 草拟裁决书为裁决书核阅模块

| 元素           | 规则                                                       |
| ------------ | -------------------------------------------------------- |
| 图标           | 玫红色（rose）背景，文件编辑图标                                       |
| 待办总数（右上角大字）  | 待草拟裁决书数量                                                 |
| 统计网格（2 列）    | **待草拟**（玫红色）：状态为待核阅的裁决书核阅数<br />**已提交**（绿色）：状态为已提交核阅的裁决书核阅数 |
| 点击跳转         | → 草拟裁决书列表（`draftAwardList`）                              |

#### 7.2.7 统计规则汇总

| 卡片    | 待办总数统计规则                                          | 辅助统计指标                              |
| ----- | ------------------------------------------------- | ----------------------------------- |
| 待开庭提醒 | 今日开庭数 + 临近待排期数                                    | 今日 / 临近 / 已排期；本周开庭完成率               |
| 声明承诺书 | `COUNT(状态 = 未签署)`                                | 待签署 / 已签署                           |
| 笔录签名  | `COUNT(状态 = 未签名)`                                | 待签署 / 已签署                           |
| 延期审批  | `COUNT(状态 = pending)`                            | 待审批 / 已审批（approved + rejected）      |
| 文书签名  | `COUNT(状态 = 待签)` + 偏移量                            | 待签 / 本月已签；本年度应签数、已签数、年度完成率          |
| 草拟裁决书 | `COUNT(状态 = 待草拟)`                                | 待草拟 / 已提交                           |

## 八、待开庭提醒

### 8.1 页面结构

| 区块  | 规则                               |
| --- | -------------------------------- |
| 搜索区 | 按案号、申请人、被申请人、办案秘书、开庭用途、开庭地点关键词搜索 |
| 列表区 | 仅展示待开庭记录，按开庭时间升序排列               |

### 8.2 卡片展示规则

| 字段   | 规则                    |
| ---- | --------------------- |
| 案号   | 完整展示                  |
| 申请人  | 名称展示                  |
| 被申请人 | 名称展示                  |
| 开庭时间 | 高亮展示，YYYY-MM-DD HH:mm |
| 开庭地点 | —                     |
| 办案秘书 | 姓名                    |
| 开庭用途 | 开庭审理 / 质证开庭 / 辩论开庭等   |

### 8.3 交互规则

- 点击案件卡片 → 进入案件详情。

## 九、声明承诺书

### 9.1 列表页结构

| 区块     | 规则                                  |
| ------ | ----------------------------------- |
| 搜索区    | 按案号、当事人、办案秘书关键词搜索                   |
| 状态 Tab | 全部 / 待签署（带计数） / 已签署                 |
| 列表区    | 卡片展示：案号、状态徽章、申请人/被申请人、办案秘书、仲裁员、签署日期<br />查询规则：查询该仲裁员名下的待签署声明承诺书列表，已签署列表按签署日期降序排序 |
| 操作     | 点击卡片进入签署详情页                         |

### 9.2 签署详情页字段规则

| 分区     | 字段/选项    | 组件   |  必填  | 规则                                                                              |
| ------ | -------- | ---- | :--: | ------------------------------------------------------------------------------- |
| 仲裁员信息  | 姓名       | 只读   | 系统带出 | 当前登录仲裁员姓名                                                                       |
| 仲裁员信息  | 手机       | 只读   | 系统带出 | 绑定手机号                                                                           |
| 仲裁员信息  | 工作单位     | 只读   | 系统带出 | 档案中的工作单位                                                                        |
| 当事人信息  | 申请人企业名称  | 只读   | 系统带出 | —                                                                               |
| 当事人信息  | 被申请人企业名称 | 只读   | 系统带出 | —                                                                               |
| 当事人信息  | 机构代码     | 只读   | 系统带出 | —                                                                               |
| 当事人信息  | 代理人      | 只读   | 系统带出 | —                                                                               |
| 当事人信息  | 地址       | 只读   | 系统带出 | —                                                                               |
| 仲裁员声明  | 声明选项     | 单选   |   是  | 4 选 1：(1) 不存在回避事由，接受担任；(2) 存在附件第 \_\_ 项回避事由，不接受；(3) 存在需披露情形（文本域）；(4) 存在其他事由，不接受 |
| 声明选项 2 | 回避事由项号   | 输入框  | 条件必填 | 选择声明选项 2 时必填；可查阅回避事由清单弹窗（含 10 项法定回避情形）                                          |
| 声明选项 3 | 披露情形描述   | 多行文本 | 条件必填 | 选择声明选项 3 时必填；并区分 3.1（不影响公正，接受）/ 3.2（影响公正，不接受）                                   |
| 保密义务   | 条款文本     | 只读   |   —  | 展示保密义务 / 本人确知 / 本人承诺条款                                                          |
| 验证     | 手写签名    | Canvas 画板  |   是  | 手写签名内容 |

### 9.3 交互规则

**提交确认：**

- 校验全部必填项（含条件必填）→ 失败则定位到首个错误项。
- 校验手写签名 → 失败则提示 `请先完成手写签名`。
- 校验通过 → 提交签署，按钮进入 loading 状态。
- 签署成功 → 归入本案数字一案一档，Toast `声明承诺书签署成功`。

## 十、笔录签名

### 10.1 列表页结构

| 区块     | 规则                                  |
| ------ | ----------------------------------- |
| 搜索区    | 按案号、申请人、被申请人、办案秘书关键词搜索              |
| 状态 Tab | 全部 / 待签名 / 已签名（带计数）                 |
| 列表区    | 卡片展示：案号、状态徽章、申请人/被申请人、办案秘书、仲裁员、开庭时间 |
| 操作     | 点击卡片进入签名详情页                         |

### 10.2 签名详情页字段规则

| 分区     | 字段/元素 | 组件        |  必填 | 规则                                  |
| ------ | ----- | --------- | :-: | ----------------------------------- |
| 案件信息卡片 | 案号    | 只读        |  —  | —                                   |
| 案件信息卡片 | 状态    | 只读        |  —  | 待签名 / 已签名                           |
| 案件信息卡片 | 当事人   | 只读        |  —  | 申请人/被申请人                            |
| 案件信息卡片 | 办案秘书  | 只读        |  —  | —                                   |
| 案件信息卡片 | 开庭时间  | 只读        |  —  | YYYY-MM-DD HH:mm                    |
| 签名面板   | 手写签名  | Canvas 画板 |  是  | HTML5 Canvas，支持鼠标和触摸手写；`清除` 按钮可重置画板 |

### 10.3 交互规则

| 操作   | 规则                                               |
| ---- | ------------------------------------------------ |
| 清除签名 | 点击后清空 Canvas 画板内容                                |
| 预览文件 | 点击 `预览文件` → 弹出笔录预览弹窗（展示案件基本信息）                   |
| 提交签名 | 校验 Canvas 是否已签名 → 未签名时提示 `请先完成手写签名`；校验通过后状态变为已签名 |


## 十一、延期审批

### 11.1 列表页结构

| 区块     | 规则                                          |
| ------ | ------------------------------------------- |
| 搜索区    | 按案号、申请人、被申请人、办案秘书关键词搜索                      |
| 状态 Tab | 全部 / 待审批 / 已审批（带计数）                         |
| 列表区    | 卡片展示：案号、状态徽章（待审批/已审批）、当事人、办案秘书、仲裁员 |
| 操作     | 点击卡片进入审批详情页                                 |

### 11.2 审批详情页字段规则

| 分区     | 字段     | 组件 |  必填 | 规则               |
| ------ | ------ | -- | :-: | ---------------- |
| 案件信息卡片 | 案号     | 只读 |  —  | —                |
| 案件信息卡片 | 当事人    | 只读 |  —  | 申请人/被申请人         |
| 案件信息卡片 | 办案秘书   | 只读 |  —  | —                |
| 延期申请信息 | 延期原因   | 只读 |  —  | 当事人填写的延期理由       |
| 延期申请信息 | 原定开庭时间 | 只读 |  —  | YYYY-MM-DD HH:mm |
| 延期申请信息 | 申请延期至  | 只读 |  —  | YYYY-MM-DD HH:mm |
| 延期申请信息 | 申请时间   | 只读 |  —  | YYYY-MM-DD HH:mm |

### 11.3 审批流转记录

时间轴展示，每条记录含：

| 字段   | 规则               |
| ---- | ---------------- |
| 操作人  | 姓名 + 角色          |
| 时间   | YYYY-MM-DD HH:mm |
| 状态   | 待审批 / 已审批  |
| 审批意见 | 文本内容（如有）         |

### 11.4 交互规则

**同意延期：**

- 触发入口：详情页底部 `同意延期`（绿色）按钮。
- 点击后弹出审批弹窗：展示案号与申请延期至摘要，提供审批意见输入框（选填，最多 300 字）。
- 确认后 → 状态变为 `已审批`，写入审批流转记录。
- Toast 提示：`已同意延期申请`。

**驳回申请：**

- 触发入口：详情页底部 `驳回申请`（红色）按钮。
- 点击后弹出驳回弹窗：提供驳回原因输入框（必填，最多 500 字，未填写时确认按钮禁用）。
- 确认后 → 状态变为 `已审批`，写入审批流转记录。
- Toast 提示：`已驳回延期申请`。

## 十二、文书签名

### 12.1 列表页结构

| 区块     | 规则                                             |
| ------ | ---------------------------------------------- |
| 搜索区    | 按案号、当事人、办案秘书、文书标题关键词搜索                         |
| 状态 Tab | 全部 / 待签名 / 已签名（带计数）                            |
| 列表区    | 卡片展示：案号、状态徽章、当事人、办案秘书、仲裁员、文书标题（仲裁裁决书/调解书）、签名日期 |
| 操作     | 点击卡片进入案件详情（文书签名 Tab）进行 GDCA 签章                   |

### 12.2 交互规则

- 点击列表卡片 → 跳转案件详情页的 `文书签名` Tab。
- 签名流程同 6.4 文书签名流程。

## 十三、草拟裁决书

- 该模块为裁决书核阅模块，流程同 6.5 裁决书核阅流程。

### 13.1 列表页结构

| 区块     | 规则                            |
| ------ | ----------------------------- |
| 搜索区    | 按案号、当事人、办案秘书、案由关键词搜索          |
| 状态 Tab | 全部 / 待草拟 / 已草拟（带计数）           |
| 列表区    | 卡片展示：案号、状态徽章、当事人、办案秘书、案由、草拟日期 |
| 操作     | 点击卡片进入案件详情（裁决书核阅 Tab）         |

### 13.2 交互规则

- 点击列表卡片 → 跳转案件详情页的 `裁决书核阅` Tab。
- 草拟流程同 6.5 裁决书核阅流程。

## 十四、案件讨论

### 14.1 页面结构

| 区块      | 规则                                                          |
| ------- | ----------------------------------------------------------- |
| 顶部标识区   | `在案合议交流室` 标识，绿色脉动指示灯                                        |
| 案件/庭室切换 | 下拉选择器，切换不同案件的讨论室（显示为 `XX 号庭`）                               |
| 聊天消息流   | 区分自己（右侧蓝色气泡）与他人（左侧白色气泡）；含发送人角色标签（办案秘书/合议庭成员/首席仲裁员）、头像、内容、时间 |
| 快捷回复区   | 底部预设常用回复短语，点击直接发送                                           |
| 自定义输入区  | 文本输入框 + 发送按钮（纸飞机图标）                                         |

### 14.2 交互规则

- 自动滚动到最新消息。
- 点击快捷回复短语 → 立即发送该短语。
- 输入文本后点击发送按钮 → 发送消息，清空输入框。

## 十五、统计中心

### 15.1 页面结构

| 区块  | 规则                                             |
| --- | ---------------------------------------------- |
| 筛选区 | 支持年度、季度筛选（全年/一季度/二季度/三季度/四季度） |
| 图表区 | 4 个统计图表                                        |

### 15.2 图表展示规则

| 图板           | 类型   | 展示内容                                                     |
| ------------ | ---- | -------------------------------------------------------- |
| 组庭情况         | 环形图  | 独任/首席/边裁三类案件数及占比，环心显示总案件数                                |
| 办理案件 Top5 案由 | 进度条  | 横向进度条列表（TOP5案由），显示件数与占比     |
| 办理案件情况       | 指标卡片 | 结案率、调撤率、延期率三项指标，含同比涨跌（▲绿色 / ▼红色）                         |
| 裁调撤情况        | 柱状图  | 四季度 × 三类指标（裁决率-蓝、调解率-橙、撤案率-绿），悬浮/点击高亮显示 Tooltip，可清除单季度筛选 |

### 15.3 交互规则

- 切换年度/季度 → 所有图表数据实时刷新。
- 点击柱状图某季度 → 高亮该季度数据，显示 Tooltip。
- 再次点击或点击空白 → 清除单季度筛选。

## 十六、消息通知

### 16.1 页面结构

| 区块    | 规则                |
| ----- | ----------------- |
| Tab 区 | 全部 / 未读（未读数量红色徽章） |
| 操作区   | 有未读时显示 `全部已读` 按钮  |
| 列表区   | 通知列表，按时间倒序        |

### 16.2 通知展示规则

| 字段   | 规则                                         |
| ---- | ------------------------------------------ |
| 通知类别 | 系统通知（红色图标）/ 业务提醒（蓝色图标）/ 签章合议（琥珀色图标）        |
| 标题   | 通知标题                                       |
| 内容   | 通知正文摘要（审理期限届满预警、庭审时段变更、裁决书合签通知、当事人补充质证提交等） |
| 时间   | 发送时间                                       |
| 未读标识 | 未读通知左侧靛蓝色强调条 + 圆点指示                        |

### 16.3 交互规则

| 操作   | 规则                  |
| ---- | ------------------- |
| 全部已读 | 一键将所有未读通知标记为已读，按钮消失 |
| 点击通知 | 切换该通知已读/未读状态        |
| 空状态  | 展示 `空空如也` 占位        |

## 十七、酬金单

### 17.1 页面结构

| 区块     | 规则                                |
| ------ | --------------------------------- |
| 搜索区    | 按案号、经办秘书关键词搜索                     |
| 状态 Tab | 全部 / 已发酬金（绿色）/ 待发酬金（琥珀色）（带计数）     |
| 列表区    | 卡片展示：案号、状态徽章、经办秘书、税前酬金金额（¥ 千分位格式） |

### 17.2 报酬规则弹窗

| 分区     | 规则                                                                                        |
| ------ | ----------------------------------------------------------------------------------------- |
| 报酬计算标准 | 按争议金额分档：100 万以下 3000-5000 元、100-500 万 5000-8000 元、500 万以上 8000-15000 元；涉外/特殊案件可上浮 20%-50% |
| 发放时间规定 | 结案签裁决书后 15 个工作日内                                                                          |
| 税务处理   | 依法代扣代缴个人所得税                                                                               |
| 特殊情况处理 | 中止/撤回/和解按工作量比例；主动回避或违纪解聘不予发放                                                              |

### 17.3 交互规则

- 点击 `报酬规则说明` → 弹出报酬规则弹窗。
- 列表为空时展示空状态提示。

## 十八、我的聘书

### 18.1 页面结构

- 存在多个聘书，点击聘书预览PDF，支持下载



## 十九、个人中心

### 19.1 个人主页结构

| 区块    | 规则                                                   |
| ----- | ---------------------------------------------------- |
| 头部区   | 头像（可上传/更换）、姓名、聘用职称徽章（特级）、所属单位、专业特长标签                 |
| 酬金单入口 | 卡片形式，点击跳转酬金单页面                                       |
| 个人信息  | 手风琴展开：姓名、职称、手机、邮箱、三类地址（含 `首选` 标记）、专业特长 → 编辑入口        |
| 工作单位  | 手风琴展开：单位名称、现任职务、行业资质 → 编辑入口                          |
| 个人履历  | 手风琴展开：学历与执业经历时间轴、核心代表著作                              |
| 银行账号  | 手风琴展开：仿真银行卡卡片（开户银行、支行、卡号、户名）→ 编辑入口 |
| 退出登录  | 二次确认弹窗，确认退出登录后跳转登录页                                  |

### 19.2 编辑页字段规则

| 编辑页    | 字段   | 组件  |  必填 | 规则                                         |
| ------ | ---- | --- | :-: | ------------------------------------------ |
| 个人信息编辑 | 中文姓名 | 输入框 |  是  | —                                          |
| 个人信息编辑 | 聘用职称 | 输入框 |  是  | —                                          |
| 个人信息编辑 | 手机号  | 输入框 |  是  | 11 位数字                                     |
| 个人信息编辑 | 联系邮箱 | 输入框 |  否  | 邮箱格式校验                                     |
| 个人信息编辑 | 居住地址 | 输入框 |  否  | 可 `设为首选`                                   |
| 个人信息编辑 | 联系地址 | 输入框 |  否  | 可 `设为首选`                                   |
| 个人信息编辑 | 其他地址 | 输入框 |  否  | 可 `设为首选`                                   |
| 工作单位编辑 | 工作单位 | 输入框 |  是  | —                                          |
| 工作单位编辑 | 现任职务 | 输入框 |  是  | —                                          |
| 工作单位编辑 | 行业资质 | 输入框 |  否  | —                                          |
| 银行账号编辑 | 开户银行 | 输入框 |  是  | 黄色 `重要提示`：银行账户变更需携带身份证、特级聘书及广州 CA 盾到现场柜台办理 |
| 银行账号编辑 | 开户支行 | 输入框 |  是  | —                                          |
| 银行账号编辑 | 银行卡号 | 输入框 |  是  | 数字                                         |
| 银行账号编辑 | 账户户名 | 输入框 |  是  | —                                          |

### 19.3 交互规则

| 操作      | 规则                                            |
| ------- | --------------------------------------------- |
| 上传/更换头像 | 点击头像 → 选择图片上传                                 |
| 设为首选地址  | 点击地址旁 `设为首选` → 切换首选标记，同时取消其他地址的首选标记           |
| 进入编辑页   | 点击分区中的 `编辑` 入口 → 进入对应编辑页                      |
| 保存修改    | 校验必填项 → 通过后保存并返回个人主页，Toast `保存成功`             |
| 取消编辑    | 点击 `取消` → 不保存直接返回个人主页                         |
| 退出登录    | 点击 `退出登录` → 弹出二次确认弹窗（提示清除 CA 核验口令）→ 确认后退出至登录页 |

## 二十、状态流转与审批规则

### 20.1 案件状态

| 状态值          | 展示文案 | 标签颜色 | 说明        |
| ------------ | ---- | ---- | --------- |
| `processing` | 审理中  | 蓝色   | 案件审理进行中   |
| `pending`    | 待开庭  | 琥珀色  | 已排期等待开庭   |
| `closed`     | 已结案  | 绿色   | 案件审理终结，终态 |
| `signing`    | 待签名  | 红色   | 文书待签名     |

### 20.2 延期审批状态流转

| 当前状态 | 触发动作 | 下一状态 | 触发方 | 说明        |
| ---- | ---- | ---- | --- | --------- |
| 待审批  | 同意延期 | 已审批  | 仲裁员 | 需可选填写审批意见 |
| 待审批  | 驳回申请 | 已驳回  | 仲裁员 | 需必填驳回原因   |

### 20.3 声明承诺书状态流转

| 当前状态 | 触发动作 | 下一状态 | 触发方 | 说明       |
| ---- | ---- | ---- | --- | -------- |
| 待签署  | 提交确认 | 已签署  | 仲裁员 | 需短信验证码验证 |

### 20.4 笔录签名状态流转

| 当前状态 | 触发动作 | 下一状态 | 触发方 | 说明              |
| ---- | ---- | ---- | --- | --------------- |
| 待签名  | 提交签名 | 已签名  | 仲裁员 | 需完成 Canvas 手写签名 |

### 20.5 文书签名状态流转

| 当前状态 | 触发动作 | 下一状态 | 触发方 | 说明                     |
| ---- | ---- | ---- | --- | ---------------------- |
| 待签名  | 确认签名 | 已签名  | 仲裁员 | 需短信/邮箱验证码 + CA 数字证书盾核签 |

### 20.6 草拟裁决书状态流转

| 当前状态 | 触发动作 | 下一状态 | 触发方 | 说明      |
| ---- | ---- | ---- | --- | ------- |
| 待草拟  | 提交上传 | 已草拟  | 仲裁员 | 上传裁决书附件 |

## 二十一、异常与边界场景

| 场景            | 处理规则                      |
| ------------- | ------------------------- |
| 登录账号或密码为空     | 阻断登录，定位到首个空字段             |
| 登录密码错误        | 提示 `账号或密码错误`              |
| 声明承诺书未选声明选项   | 阻断提交，定位到声明选项区             |
| 声明承诺书短信验证码错误  | 阻断提交，提示 `验证码错误或已过期`       |
| 笔录签名未完成手写签名   | 阻断提交，提示 `请先完成手写签名`        |
| 延期审批驳回未填驳回原因  | 阻断提交，确认按钮保持禁用状态           |
| 文书签名验证码错误     | 阻断签名，提示 `验证码错误或已过期`       |
| CA 数字证书盾验证失败  | 阻断签章操作，提示 `CA 证书验证失败，请重试` |
| 案件列表无搜索结果     | 展示空状态 + `清空搜索与过滤` 按钮      |
| 待开庭列表为空       | 展示空状态提示                   |
| 消息通知列表为空      | 展示 `空空如也` 占位              |
| 酬金单列表为空       | 展示空状态提示                   |
| 材料审阅未全部阅读即提交  | 阻断提交，提示 `请先阅读全部材料`        |
| 个人信息编辑手机号格式错误 | 阻断保存，定位到手机号字段             |
| 个人信息编辑邮箱格式错误  | 阻断保存，定位到邮箱字段              |
| 银行账号编辑卡号非数字   | 阻断保存，定位到卡号字段              |
| 前端重复点击提交      | 提交后按钮立即禁用，不可重复点击          |
| 后端重复接收同一次提交   | 按幂等规则处理，不得生成重复记录          |
| 导出聘书 PDF 失败   | 提示 `导出失败，请重试`             |

## 二十二、待确认事项

| 编号   | 问题                       | 当前状态    |
| ---- | ------------------------ | ------- |
| T-01 | 智云数字法庭是否本期上线，还是作为概念展示    | 待产品确认   |
| T-02 | 裁决书全文在线编辑能力排期            | 待产品确认   |

| T-03 | 消息通知是否需要接入微信小程序订阅消息推送    | 待产品确认   |
| T-04 | 案件讨论即时通讯是否需要历史消息持久化与分页加载 | 待产品确认   |

